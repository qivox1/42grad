---
interface Props {
  title: string;
  subtitle?: string;
  label?: string;
  image?: string;   // e.g. "kv-plattform.webp" — relative to /images/
}

const { title, subtitle, label, image } = Astro.props;
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const imageSrc = image ? `${base}/images/${image}` : null;
---

<section class={`page-hero${imageSrc ? ' page-hero--img' : ''}`}>
  {imageSrc && (
    <div class="page-hero-bg">
      <img src={imageSrc} alt="" class="page-hero-img" aria-hidden="true" loading="eager" fetchpriority="high" />
      <div class="page-hero-overlay"></div>
    </div>
  )}
  <div class="container page-hero-content">
    {label && <span class="section-label">{label}</span>}
    <h1 class="page-hero-title" set:html={title} />
    {subtitle && <p class="page-hero-subtitle">{subtitle}</p>}
  </div>
</section>

<style>
  .page-hero {
    padding: 120px 0 70px;
    background: linear-gradient(180deg, #f0f4f8 0%, #fff 100%);
    position: relative;
    overflow: hidden;
  }

  /* ── With background image ── */
  .page-hero--img {
    background: none;
    min-height: 340px;
  }

  .page-hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .page-hero-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 30%;
    display: block;
  }

  /* Left-heavy gradient: fully opaque → fades out to the right */
  .page-hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      105deg,
      rgba(255, 255, 255, 0.97) 0%,
      rgba(255, 255, 255, 0.93) 35%,
      rgba(255, 255, 255, 0.75) 55%,
      rgba(255, 255, 255, 0.35) 78%,
      rgba(255, 255, 255, 0.10) 100%
    );
  }

  /* ── Content ── */
  .page-hero-content {
    max-width: 800px;
    position: relative;
    z-index: 1;
  }

  .page-hero-title {
    font-size: clamp(36px, 5vw, 56px);
    font-weight: 700;
    color: var(--color-dark);
    line-height: 1.15;
    margin-bottom: 20px;
  }

  .page-hero-title :global(.highlight) {
    color: var(--color-accent);
  }

  .page-hero-subtitle {
    font-size: 18px;
    color: var(--color-text-light);
    line-height: 1.7;
    max-width: 600px;
  }

  @media (max-width: 768px) {
    .page-hero--img {
      min-height: 280px;
    }
    /* On mobile cover fully so text stays readable */
    .page-hero-overlay {
      background: rgba(255, 255, 255, 0.92);
    }
  }
</style>
